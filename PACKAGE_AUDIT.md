# 智慧记图报发布审查

## 交付面

- GitHub/WorkBuddy 版：包含 README、四张效果图、开发说明、插件元数据和启动 Hook。
- SkillHub 版：包含简化的 `SKILL.md`、三条“试试这样用”、数据说明和运行文件；不包含 README、展示图、`agents/`、插件元数据或 Hook。
- 两个包的 `package.json`、`package-lock.json`、`references/` 和 `scripts/` 必须逐文件 SHA-256 一致。

## 产品边界

- 对外只提供经营日历和经营月报。
- 公开 `SKILL.md` 只保留产品介绍、两项能力和自然语言示例。
- 执行方式、数据校验、失败收口和运行边界放在 `references/runtime-workflow.md`。
- 已移除的独立查询和对账功能不得进入任何正式包。

## 发布门禁

`scripts/build-release.mjs` 会在构建前强制检查：

1. 工作区已提交且无未保存变更。
2. Skill、锁文件和 WorkBuddy 插件版本号完全一致。
3. SkillHub 公开正文不含命令、路径、安装、依赖、错误字段或代码块。
4. 两个包不含旧功能脚本、测试目录、开发目录、`node_modules`、运行缓存、本地数据或输出文件。
5. 不含符号链接、密钥、认证信息、手机号或真实经营数据。
6. 两个包的运行文件列表和哈希一致。
7. 只有所有检查通过才会写入最终 ZIP 和 `RELEASE.json`。

## 1.5.2 验证范围

- 单元与渲染测试覆盖金额精度、日期、收款、退货停止、完整分页、进货、待收、客户欠款、低库存、缺货、零数据、不覆盖和图片尺寸。
- 运行检查要求 `ailit >= 0.8.1`，并验证日历和月报所需的 14 组关键命令。
- 对外错误只使用用户语言，不返回底层错误、本地路径、安装过程或退出码。
- 启动 Hook 的标准输出与错误输出必须同时为 0 字节；即使初始化失败也不泄露过程。
- 清洁安装测试使用无 `node_modules`、Skill 目录只读和独立缓存的解包环境。
- 正式交付的文件数、字节数和 SHA-256 以交付目录中的 `RELEASE.json` 为准。
- 商品销量和利润排行保留原有左右布局与横条样式；商品名限制在独立区域，按宽度缩字或换行，极长名称才省略。
- 首屏主力商品、库存提醒及客户欠款等文字区域一并检查，避免名称压住相邻数据。
